/*
08 - Logical Operators
Used to combine conditions.
*/
const hasUsername = true;
const hasPassword = false;
console.log(hasUsername && hasPassword); // true only if both are true
console.log(hasUsername || hasPassword); // true if at least one is true
console.log(!hasUsername); // Reverses true to false
